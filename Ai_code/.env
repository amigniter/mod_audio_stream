WSS_PEM=""
OPENAI_API_KEY="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"
HOST="0.0.0.0"
PORT="8765"

# Optional: override realtime model/voice
# OPENAI_REALTIME_MODEL="gpt-4o-realtime-preview"
# OPENAI_REALTIME_VOICE="alloy"

# Optional: allow insecure TLS (DEV ONLY)
# OPENAI_WSS_INSECURE="0"
FS_SAMPLE_RATE="8000"
FS_OUT_SAMPLE_RATE="8000"
FS_CHANNELS="1"
FS_FRAME_MS="20"

# ---- Playout tuning (audio smoothness vs latency) ----
# If audio sounds choppy/robotic, try increasing PREBUFFER a little (80-120).
# If latency is too high, lower MAX_BUFFER.
PLAYOUT_PREBUFFER_MS="80"
PLAYOUT_MAX_BUFFER_MS="800"
PLAYOUT_CATCHUP_MAX_MS="120"
PLAYOUT_SLEEP_GRANULARITY_MS="2"

# Throttle response.create calls (ms). Lower = more responsive, higher = safer.
RESPONSE_MIN_INTERVAL_MS="400"

# ---- OpenAI Realtime tuning (recommended for SIP/telephony) ----
# Resample 8k telephony audio -> 16k before sending to OpenAI (more reliable).
OPENAI_RESAMPLE_INPUT="1"
OPENAI_INPUT_SAMPLE_RATE="16000"

# OpenAI audio output is typically 16k. Some events omit sample_rate; use this as default.
OPENAI_OUTPUT_SAMPLE_RATE="16000"

# Commits are VAD-driven (recommended). Keep this OFF.
OPENAI_FORCE_COMMIT_MS="0"

# Send OpenAI output back to FreeSWITCH using your mod_audio_stream JSON schema.
FS_SEND_JSON_AUDIO="1"
FS_SEND_JSON_HANDSHAKE="0"