# FreeSWITCH -> this server (mod_audio_stream connects here)
IVR_HOST=0.0.0.0
IVR_PORT=8765

# OpenAI Realtime
OPENAI_API_KEY=replace_me
OPENAI_REALTIME_MODEL=gpt-4o-realtime-preview
OPENAI_REALTIME_VOICE=alloy

# TLS CA bundle for outbound OpenAI WSS (optional). Must NOT contain a private key.
# If you want to reuse the provided file, set:
# WSS_PEM=./wss.pem

# Dev escape hatch (NOT recommended)
# OPENAI_WSS_INSECURE=0

# Audio tuning (bridge assumes FS sends 20ms pcm16 frames)
FS_FRAME_MS=20
FS_SAMPLE_RATE=16000
FS_CHANNELS=1

# Playback smoothing/buffering
PLAYOUT_PREBUFFER_MS=300
PLAYOUT_MAX_BUFFER_MS=8000
PLAYOUT_CATCHUP_MAX_MS=120
PLAYOUT_SLEEP_GRANULARITY_MS=2

# If 1: send JSON/base64 frames to FreeSWITCH; if 0: send raw binary PCM frames
# For compatibility with mod_audio_stream README 'play' feature, JSON is recommended.
FS_SEND_JSON_AUDIO=1
FS_SEND_JSON_HANDSHAKE=1

# Optional: send a short beep to the caller as soon as the WS connects
SEND_TEST_TONE=0
