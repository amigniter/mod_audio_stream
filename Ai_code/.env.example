HOST="0.0.0.0"
PORT="8765"

WSS_PEM="./wss.pem"

OPENAI_API_KEY=""
OPENAI_REALTIME_MODEL="gpt-4o-realtime-preview"
OPENAI_REALTIME_VOICE="alloy"

FS_FRAME_MS="20"
FS_SAMPLE_RATE="8000"
FS_CHANNELS="1"
FS_OUT_SAMPLE_RATE="24000"

OPENAI_RESAMPLE_INPUT="1"
OPENAI_INPUT_SAMPLE_RATE="24000"
OPENAI_OUTPUT_SAMPLE_RATE="24000"

PLAYOUT_PREBUFFER_MS="100"
PLAYOUT_TARGET_BUFFER_MS="200"
PLAYOUT_MAX_DRAIN_FRAMES="2"
PLAYOUT_MAX_BUFFER_MS="2000"
PLAYOUT_CATCHUP_MAX_MS="80"
PLAYOUT_SLEEP_GRANULARITY_MS="2"
RESPONSE_MIN_INTERVAL_MS="400"

OPENAI_ITEM_MAX_BUFFER_MS="12000"

FS_SEND_JSON_AUDIO="1"
FS_SEND_JSON_HANDSHAKE="1"

SEND_TEST_TONE="0"

# ─────────────────────────────────────────────────────────
#  CUSTOM VOICE — Make the AI sound exactly like YOUR voice
# ─────────────────────────────────────────────────────────
#
#  Steps to use your voice (ww.wav):
#    1. Go to https://elevenlabs.io/app/voice-lab
#    2. Click "Add Generative or Cloned Voice" → "Instant Voice Clone"
#    3. Upload your ww.wav file
#    4. Name it (e.g., "My Voice") → click "Add Voice"
#    5. Copy the Voice ID from the voice card
#    6. Set TTS_PROVIDER, TTS_API_KEY, TTS_VOICE_ID below
#
#  Set TTS_PROVIDER="none" to use OpenAI's built-in voice instead.
# ─────────────────────────────────────────────────────────

# Provider: elevenlabs | cartesia | selfhosted | openai | none
TTS_PROVIDER="none"

# Your ElevenLabs API key (https://elevenlabs.io/app/settings/api-keys)
TTS_API_KEY=""

# Voice ID from your cloned voice
TTS_VOICE_ID=""

# Model: eleven_turbo_v2_5 (fastest) | eleven_multilingual_v2 (highest quality)
TTS_MODEL="eleven_turbo_v2_5"

# Failover if primary TTS fails
TTS_FALLBACK_PROVIDER="openai"

# Sentence buffering
TTS_SENTENCE_MAX_CHARS="80"
TTS_SENTENCE_MIN_CHARS="10"

# Phrase caching (0ms for repeated phrases)
TTS_CACHE_ENABLED="1"
TTS_CACHE_MAX_ENTRIES="500"
TTS_CACHE_TTL_S="3600"

# Self-hosted TTS server URL (if using XTTS, Fish Speech, etc.)
# TTS_SELFHOSTED_URL="http://localhost:8080"
